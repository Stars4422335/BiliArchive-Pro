# 🚀 GitHub 代理加速节点参考手册

如果您在国内环境下载组件（yt-dlp/ffmpeg）缓慢，请在 `config.yaml` 的 `github_proxy_url` 中填入以下前缀。

## 推荐镜像源
- `https://mirror.ghproxy.com/` (稳定)
- `https://ghproxy.net/` (快速)
- `https://kkgithub.com/` (备用)

**使用方法：** 复制链接，填入配置文件的对应项即可。