# BiliArchive-Pro 开发维护指南

## 核心逻辑说明
1. **资产保护**：如果本地已有视频但 B 站删除了，程序会重命名文件夹并标记为 `[源端已删]`，绝对不会覆盖。
2. **墓碑机制**：如果收藏夹里有失效视频且本地没下过，程序会建立一个包含元数据的“墓碑”文件夹，方便以后“复活”下载。
3. **Plex 兼容**：所有视频下载后都会生成对应的 `.nfo` 文件。

## 目录职能
- `app/core/`: 处理下载、解析、元数据。
- `app/scheduler/`: 处理定时扫描和自动更新。